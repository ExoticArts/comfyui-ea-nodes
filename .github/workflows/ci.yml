name: CI

on:
  push:
    branches: [ main ]
    paths:
      - "nodes/**"
      - "tests/**"
      - ".github/workflows/**"
      - "requirements.txt"
      - "README.md"
  pull_request:
    branches: [ main ]
    paths:
      - "nodes/**"
      - "tests/**"
      - ".github/workflows/**"
      - "requirements.txt"
      - "README.md"
  workflow_dispatch:

jobs:
  validate:
    name: Validate EA Nodes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install requirements (if present)
        run: |
          if [ -f requirements.txt ] && [ -s requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Run validator
        run: python tests/validate_ea_nodes.py
